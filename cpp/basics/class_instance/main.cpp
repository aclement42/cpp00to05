#include "Sample.class.hpp"

int main()
{
    Sample instance;
    //var instance est de type Sample, c'est une instance de la classe Sample
    //je fais donc un appel au code du constructeur
    return (0);
}
//toutes les var locales sont detruites, donc je fais appel a la fin de ma fonction au code du destructeur
